let a = "rock";
let b = "paper";
let c = "scissors";

function computerPlay() {
	let computerRoll = Math.floor(Math.random() * 3); 
	if (computerRoll === 0) 
		return a;
	else if (computerRoll === 1)
		return b;
	else
		return c; 
}

const computerSelection = computerPlay ();

const playerSelection = prompt ();




function playRound(playerSelection, computerSelection) {
	let result = (playerSelection === a && computerSelection === b)
	if (result = true);
		return (++computerWin);
		alert ("Computer wins!"); }
	
	
	
	
	
	
	let computerWin = 0;
	let playerWin = 0;
	if (playerSelection === a && computerSelection === b) 
		return (++computerWin);
		alert ("Computer wins!");
		
	
}



func game()

let counter = 1;
let a = ++counter;

console.log()

